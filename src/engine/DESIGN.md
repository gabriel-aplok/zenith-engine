# Engine Design Note

This file no longer describes the active direction of the repository.

The previous design document assumed a much more advanced renderer and scene stack than the current reset state supports.

Use this document instead:

- [../../docs/backend-agnostic-rebuild.md](../../docs/backend-agnostic-rebuild.md)

That rebuild plan is the source of truth for:

- backend-agnostic application flow
- window and input abstraction boundaries
- renderer split strategy
- clean-slate rebuild phases
